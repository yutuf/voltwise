services:
  voltwise:
    build: .
    ports:
      - "8000:8000"
    environment:
      - SECRET_KEY=change-me-in-production
      - DATABASE_URL=sqlite:///./voltwise.db
    volumes:
      - voltwise-data:/app/voltwise.db
    restart: unless-stopped

volumes:
  voltwise-data:
